import React, { FC } from 'react'
import { RouteChildrenProps } from 'react-router-dom'

const FlowOrder: FC<RouteChildrenProps> = (props) => {
    return (
        <div>FlowOrder</div>
    )
}

export default FlowOrder